# missionctl

Mission Control CLI for OpenClaw agents and dispatcher workflows.

## Quickstart

```bash
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -e ".[dev]"
missionctl doctor
```

Use `--format json` for machine-friendly output in dispatcher/agent flows.
